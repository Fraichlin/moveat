{% extends 'baseFrontend.html.twig' %}
{% block title %}Coach Home{% endblock %}

{% block body %}
    <div style="border: 1px solid orangered;background-color: #f1d494;">
        <h1>Bienvenue {{ app.user.getLogin }}</h1><br>
        {% if not app.user.getPhoto %}
            <button class="btn btn-default" >Pas de photo de profil</button>
        {% else %}
            <img src="{{ asset( 'upload/images/' ~ app.user.getPhoto ) }}" class="img-circle" width="80">
        {% endif %}
        <a class="btn btn-info" href="{{ url('updateProfilCoach',{'id':app.user.getId}) }}">
            Modifier mon profil
        </a>
        <a class="btn btn-success" href="{{ url('logoutCoach') }}">Se deconnecter</a>
    </div>
{% endblock %}
