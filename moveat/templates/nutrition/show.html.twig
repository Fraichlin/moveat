{% extends 'base2.html.twig' %}

{% block title %}Nutrition{% endblock %}

{% block body %}
    <h1>Nutrition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nutrition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nutrition.name }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ nutrition.lastName }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ nutrition.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ nutrition.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ nutrition.email }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ nutrition.image }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('nutrition_index') }}">back to list</a>

    <a href="{{ path('nutrition_edit', {'id': nutrition.id}) }}">edit</a>

    {{ include('nutrition/_delete_form.html.twig') }}
{% endblock %}
